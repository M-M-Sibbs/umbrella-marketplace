# Copy to .env.local for local dev (optional — app works without these locally).
# REQUIRED on Vercel (set them in the Vercel dashboard, not in a file).

TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token
JWT_SECRET=change-this-to-a-long-random-string
